═══════════════════════════════════════════════════════════════
🚀 EXECUTAR SOLUÇÃO 403 - COMANDO COMPLETO
═══════════════════════════════════════════════════════════════

Cole este comando completo no SSH:

═══════════════════════════════════════════════════════════════

cd /home/u127271520/domains/betgeniusbr.com/public_html && echo "1️⃣ Fazendo backup..." && cp .htaccess .htaccess.backup.$(date +%Y%m%d_%H%M%S) 2>/dev/null && echo "✅ Backup criado" && echo "" && echo "2️⃣ Criando .htaccess simplificado..." && cat > .htaccess << 'EOF'
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Permitir acesso direto a arquivos e diretórios
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Redirecionar tudo para public/
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
EOF
chmod 644 .htaccess && echo "✅ .htaccess criado" && echo "" && echo "3️⃣ Verificando permissões..." && chmod 755 public/ && chmod 644 public/index.php 2>/dev/null && echo "✅ Permissões OK" && echo "" && echo "✅ PRONTO! Limpe o cache do navegador (Ctrl+Shift+R) e teste: https://betgeniusbr.com/" && echo "" && echo "⚠️  Se ainda não funcionar, altere o Document Root no hPanel:" && echo "   public_html → public_html/public"

═══════════════════════════════════════════════════════════════

