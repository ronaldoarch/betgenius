═══════════════════════════════════════════════════════════════
🔍 DIAGNÓSTICO - Por que API retorna HTML?
═══════════════════════════════════════════════════════════════

Execute estes comandos no SSH, um de cada vez:

═══════════════════════════════════════════════════════════════

1. Verificar .htaccess na RAIZ:
───────────────────────────────────────────────────────────────
cd /home/u127271520/domains/betgeniusbr.com/public_html && cat .htaccess

2. Verificar .htaccess em PUBLIC:
───────────────────────────────────────────────────────────────
cat public/.htaccess

3. Verificar se index.php e index.html existem:
───────────────────────────────────────────────────────────────
ls -la public/index.php public/index.html

4. Testar se Laravel responde diretamente:
───────────────────────────────────────────────────────────────
cd public && php -r "require 'index.php';" 2>&1 | head -c 200

5. Verificar rotas do Laravel:
───────────────────────────────────────────────────────────────
cd .. && php artisan route:list | grep api | head -5

═══════════════════════════════════════════════════════════════

Envie os resultados de TODOS os comandos acima!

═══════════════════════════════════════════════════════════════

