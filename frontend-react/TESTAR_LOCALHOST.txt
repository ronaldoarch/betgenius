═══════════════════════════════════════════════════════════════
🔧 TESTAR COM LOCALHOST - COMANDOS SIMPLES
═══════════════════════════════════════════════════════════════

Execute estes comandos um por vez:

═══════════════════════════════════════════════════════════════

1️⃣ Testar conexão com localhost:

mysql -u u911385553_boraganhar -p'2403Auror@' -h localhost u911385553_boraganhar -e "SELECT 1;"

═══════════════════════════════════════════════════════════════

2️⃣ Se funcionar, verificar .env:

cat .env | grep DB_HOST

═══════════════════════════════════════════════════════════════

3️⃣ Limpar cache:

php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
rm -f bootstrap/cache/config.php

═══════════════════════════════════════════════════════════════

4️⃣ Testar conexão via Laravel:

php artisan tinker --execute="DB::connection()->getPdo(); echo 'OK';"

═══════════════════════════════════════════════════════════════

5️⃣ Se ainda não funcionar, verificar senha no hPanel:

# Acesse: https://hpanel.hostinger.com/
# Vá em: Banco de Dados MySQL
# Verifique a senha correta
# Depois atualize no .env:

nano .env
# Procure por DB_PASSWORD e atualize

═══════════════════════════════════════════════════════════════

