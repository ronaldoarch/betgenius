═══════════════════════════════════════════════════════════════
🔧 CORRIGIR PERMISSÕES E VERIFICAR ARQUIVOS
═══════════════════════════════════════════════════════════════

Execute estes comandos:

═══════════════════════════════════════════════════════════════

1️⃣ Corrigir permissões da pasta assets:

cd /home/u127271520/domains/betgeniusbr.com/public_html/public && chmod -R 755 app/assets/ && echo "✅ Permissões corrigidas"

═══════════════════════════════════════════════════════════════

2️⃣ Listar arquivos diretamente (sem find):

ls -la app/assets/ | head -20

═══════════════════════════════════════════════════════════════

3️⃣ Procurar arquivos CSS e JS específicos:

ls -la app/assets/*.css app/assets/*.js 2>&1 | head -10

═══════════════════════════════════════════════════════════════

4️⃣ Verificar o que index.html está referenciando:

cat app/index.html | grep -E "assets|\.js|\.css"

═══════════════════════════════════════════════════════════════

5️⃣ Se os arquivos não estiverem lá, verificar se estão em build/assets:

ls -la build/assets/*.css build/assets/*.js 2>&1 | head -10

═══════════════════════════════════════════════════════════════

