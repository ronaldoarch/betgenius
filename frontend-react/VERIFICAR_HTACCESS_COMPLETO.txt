═══════════════════════════════════════════════════════════════
🔍 VERIFICAR .HTACCESS COMPLETO E TESTAR ROTEAMENTO
═══════════════════════════════════════════════════════════════

Execute no SSH:

───────────────────────────────────────────────────────────────

cd /home/u127271520/domains/betgeniusbr.com/public_html

# 1. Ver .htaccess completo na raiz
echo "=== .htaccess na raiz (COMPLETO) ==="
cat .htaccess
echo ""
echo ""

# 2. Ver .htaccess completo em public/
echo "=== .htaccess em public/ (COMPLETO) ==="
cat public/.htaccess
echo ""
echo ""

# 3. Testar se /zirigui está sendo roteado
echo "=== Testando roteamento /zirigui ==="
curl -s -I "https://betgeniusbr.com/zirigui" | head -10
echo ""

# 4. Testar se retorna HTML ou JSON
echo "=== Testando conteúdo retornado ==="
curl -s "https://betgeniusbr.com/zirigui" | head -20
echo ""

# 5. Verificar se há algum problema com o DirectoryIndex
echo "=== Verificando DirectoryIndex ==="
grep -i "DirectoryIndex" .htaccess public/.htaccess
echo ""

# 6. Testar acesso direto ao Laravel
echo "=== Testando acesso direto ao Laravel ==="
curl -s -I "https://betgeniusbr.com/public/index.php" | head -5
echo ""

═══════════════════════════════════════════════════════════════

