═══════════════════════════════════════════════════════════════
🔧 CORRIGIR ERRO 500 - ARQUIVOS ESTÁTICOS
═══════════════════════════════════════════════════════════════

O problema: .htaccess está redirecionando arquivos estáticos para Laravel

═══════════════════════════════════════════════════════════════

SOLUÇÃO: Atualizar .htaccess para servir arquivos estáticos diretamente

cd /home/u127271520/domains/betgeniusbr.com/public_html/public && cat > .htaccess << 'EOF'
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # IMPORTANTE: Permitir acesso direto a arquivos estáticos PRIMEIRO
  # Arquivos e diretórios existentes - servir diretamente
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # API Laravel
  RewriteCond %{REQUEST_URI} ^/api
  RewriteRule ^ index.php [L]

  # Admin Laravel
  RewriteCond %{REQUEST_URI} ^/admin
  RewriteRule ^ index.php [L]

  # Storage Laravel
  RewriteCond %{REQUEST_URI} ^/storage
  RewriteRule ^ - [L]

  # React está em /app/ - redirecionar raiz para lá
  RewriteCond %{REQUEST_URI} ^/$
  RewriteRule ^ app/ [L]

  # Todas as outras rotas vão para o React em /app/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_URI} !^/app/
  RewriteRule ^(.*)$ app/$1 [L]
</IfModule>

DirectoryIndex app/index.html index.php
EOF
chmod 644 .htaccess && echo "✅ .htaccess atualizado"

═══════════════════════════════════════════════════════════════

VERIFICAR PERMISSÕES:

chmod 755 app/
chmod 644 app/index.html
chmod 755 app/assets/
chmod 644 app/assets/*
echo "✅ Permissões ajustadas"

═══════════════════════════════════════════════════════════════

TESTAR:

echo "Teste no navegador:"
echo "  https://betgeniusbr.com/app/assets/index-R46pBGYW.css"
echo "  https://betgeniusbr.com/app/assets/index-DrMwNlRZ.js"
echo "  https://betgeniusbr.com/"

═══════════════════════════════════════════════════════════════

