═══════════════════════════════════════════════════════════════
🔧 TESTAR CONEXÃO COM BANCO DE DADOS
═══════════════════════════════════════════════════════════════

Execute estes comandos no SSH para diagnosticar:

═══════════════════════════════════════════════════════════════

1️⃣ Testar conexão manual com MySQL:

mysql -u u911385553_boraganhar -p'2403Auror@' -h 127.0.0.1 u911385553_boraganhar -e "SELECT 1;" && echo "✅ Conexão OK!" || echo "❌ Erro na conexão"

═══════════════════════════════════════════════════════════════

2️⃣ Se o comando acima falhar, tente com localhost:

mysql -u u911385553_boraganhar -p'2403Auror@' -h localhost u911385553_boraganhar -e "SELECT 1;" && echo "✅ Conexão OK!" || echo "❌ Erro na conexão"

═══════════════════════════════════════════════════════════════

3️⃣ Verificar se o MySQL está rodando:

systemctl status mysql || service mysql status || ps aux | grep mysql

═══════════════════════════════════════════════════════════════

4️⃣ Limpar TODOS os caches (importante!):

cd /home/u127271520/domains/betgeniusbr.com/public_html && php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear && rm -f bootstrap/cache/config.php && echo "✅ Cache limpo completamente"

═══════════════════════════════════════════════════════════════

5️⃣ Testar conexão via Laravel:

php artisan tinker --execute="try { DB::connection()->getPdo(); echo '✅ Conexão OK!'; } catch(Exception \$e) { echo '❌ Erro: ' . \$e->getMessage(); }"

═══════════════════════════════════════════════════════════════

6️⃣ Se a senha estiver errada, atualizar no .env:

# Fazer backup
cp .env .env.backup

# Editar .env (substitua pela senha correta do hPanel)
nano .env
# Procure por DB_PASSWORD e atualize

# Depois limpar cache novamente
php artisan config:clear

═══════════════════════════════════════════════════════════════

